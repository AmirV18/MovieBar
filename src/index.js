import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
//root is rendered from the index.html in public. It's a div that is rendered with what's in app.js